import {describe,it,expect} from 'vitest';import {haversineMeters,startOfChinaDay} from '../lib/utils';import {cleanPublicValue} from '../lib/public-config';import {safeUploadPath} from '../lib/uploads';import {currentWorkInfo,fullWorkYears} from '../lib/work-years';import {attendanceResult,chinaAttendanceDays,timeOnChinaDay} from '../lib/attendance';
describe('考勤距离',()=>{it('同一坐标距离为 0',()=>expect(haversineMeters({latitude:31.2,longitude:121.4},{latitude:31.2,longitude:121.4})).toBe(0));it('约 111 米纬度差',()=>expect(haversineMeters({latitude:0,longitude:0},{latitude:.001,longitude:0})).toBeGreaterThan(110))});
describe('日期',()=>{it('返回上海自然日起点',()=>expect(startOfChinaDay(new Date('2026-08-08T10:00:00Z')).toISOString()).toBe('2026-08-07T16:00:00.000Z'))});
describe('工龄和年假',()=>{it('按上海当天与入职周年计算完整工龄',()=>{const start=new Date('2021-08-10T00:00:00.000Z');expect(fullWorkYears(start,new Date('2026-08-09T04:00:00.000Z'))).toBe(4);expect(fullWorkYears(start,new Date('2026-08-10T04:00:00.000Z'))).toBe(5)});it('年假天数与完整工龄一致',()=>expect(currentWorkInfo(new Date('2021-08-10T00:00:00.000Z'),new Date('2026-08-10T04:00:00.000Z'))).toEqual({workYears:5,annualLeaveDays:5}))});
describe('考勤规则',()=>{it('迟到且早退按旷工处理',()=>expect(attendanceResult(true,true)).toEqual({status:'ABSENT',exceptionType:'ABSENT'}));it('只早退保留出勤状态并创建早退异常',()=>expect(attendanceResult(false,true)).toEqual({status:'ON_TIME',exceptionType:'EARLY_LEAVE'}));it('按上海自然日生成上下班时间',()=>expect(timeOnChinaDay(new Date('2026-08-08T16:00:00.000Z'),'09:00').toISOString()).toBe('2026-08-09T01:00:00.000Z'));it('请假日期转换成上海考勤日期',()=>expect(chinaAttendanceDays(new Date('2026-08-09'),new Date('2026-08-10')).map(x=>x.toISOString())).toEqual(['2026-08-08T16:00:00.000Z','2026-08-09T16:00:00.000Z']))});
describe('部署配置',()=>{it('清除环境变量外层引号',()=>expect(cleanPublicValue('"amap-key"')).toBe('amap-key'));it('拒绝上传目录穿越',()=>expect(safeUploadPath(['..','secret'])).toBeNull())});
