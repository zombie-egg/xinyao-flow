export function fullWorkYears(startDate:Date,now=new Date()){const todayParts=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Shanghai',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(now),value=(type:string)=>Number(todayParts.find(part=>part.type===type)?.value),year=value('year'),month=value('month'),day=value('day'),startYear=startDate.getUTCFullYear(),startMonth=startDate.getUTCMonth()+1,startDay=startDate.getUTCDate();return year-startYear-(month<startMonth||month===startMonth&&day<startDay?1:0)}
export function currentWorkInfo(startDate:Date,now=new Date()){const workYears=Math.max(0,fullWorkYears(startDate,now));return {workYears,annualLeaveDays:workYears}}
