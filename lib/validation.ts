import { z } from 'zod';
export const loginSchema=z.object({username:z.string().min(2),password:z.string().min(8)});
export const locationSchema=z.object({latitude:z.number().min(-90).max(90),longitude:z.number().min(-180).max(180),accuracy:z.number().min(0).max(5000)});
export const leaveSchema=z.object({type:z.enum(['ANNUAL','PERSONAL','BUSINESS_TRIP']),startDate:z.coerce.date(),endDate:z.coerce.date(),reason:z.string().max(1000).optional(),destination:z.string().max(200).optional(),tripBudgetRequired:z.boolean().default(false),estimatedBudget:z.number().nonnegative().optional(),remark:z.string().max(1000).optional()}).superRefine((v,c)=>{if(v.endDate<v.startDate)c.addIssue({code:'custom',message:'结束日期不能早于开始日期',path:['endDate']});if(v.type==='PERSONAL'&&!v.reason)c.addIssue({code:'custom',message:'事假原因必填',path:['reason']});if(v.type==='BUSINESS_TRIP'&&!v.destination)c.addIssue({code:'custom',message:'出差地点必填',path:['destination']});if(v.tripBudgetRequired&&v.estimatedBudget===undefined)c.addIssue({code:'custom',message:'请填写预计经费',path:['estimatedBudget']})});
export const customerSchema=z.object({name:z.string().min(2).max(100),contact:z.string().min(2).max(50),phone:z.string().regex(/^1\d{10}$/,'手机号格式不正确'),address:z.string().max(300).optional(),remark:z.string().max(1000).optional()});
export const contractSchema=z.object({customerId:z.string().min(1),contractNumber:z.string().regex(/^[A-Za-z0-9_-]{4,40}$/),name:z.string().min(2).max(150),amount:z.number().positive(),dealPrice:z.number().positive(),contractDate:z.coerce.date(),remark:z.string().max(1000).optional()});
