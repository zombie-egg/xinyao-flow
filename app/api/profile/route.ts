import {requireUser} from '@/lib/auth';import {db} from '@/lib/db';import {ok,fail,apiError} from '@/lib/api';import {writeFile,mkdir} from 'node:fs/promises';import path from 'node:path';import crypto from 'node:crypto';import {z} from 'zod';
export const runtime='nodejs';const nameSchema=z.string().trim().min(2,'名称至少需要2个字符').max(40,'名称不能超过40个字符');const allowed=new Map([['image/jpeg','jpg'],['image/png','png'],['image/webp','webp']]);
export async function PUT(req:Request){try{const user=await requireUser(),form=await req.formData(),parsed=nameSchema.safeParse(form.get('name'));if(!parsed.success)return fail(parsed.error.issues[0].message,'VALIDATION_ERROR');const avatar=form.get('avatar');let avatarUrl=user.avatarUrl;if(avatar instanceof File&&avatar.size>0){if(!allowed.has(avatar.type))return fail('头像仅支持 JPG、PNG、WEBP','INVALID_FILE_TYPE');if(avatar.size>2*1024*1024)return fail('头像不能超过 2MB','FILE_TOO_LARGE');const dir=path.join(process.cwd(),'public','uploads');await mkdir(dir,{recursive:true});const filename=`avatar-${user.id}-${crypto.randomUUID()}.${allowed.get(avatar.type)}`;await writeFile(path.join(dir,filename),Buffer.from(await avatar.arrayBuffer()));avatarUrl=`/uploads/${filename}`}const updated=await db.user.update({where:{id:user.id},data:{name:parsed.data,avatarUrl}});await db.operationLog.create({data:{userId:user.id,action:'UPDATE_PROFILE',module:'PROFILE',targetId:user.id,description:'更新个人名称或头像'}});return ok({name:updated.name,avatarUrl:updated.avatarUrl})}catch(e){return apiError(e)}}
