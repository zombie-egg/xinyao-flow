import {locationSchema} from '@/lib/validation';import {db} from '@/lib/db';import {requirePermission} from '@/lib/auth';import {ok,fail,apiError} from '@/lib/api';import {haversineMeters,startOfChinaDay} from '@/lib/utils';import {attendanceDirectAdmin,attendanceResult,timeOnChinaDay} from '@/lib/attendance';
export async function POST(req:Request){try{const user=await requirePermission('attendance:self'),p=locationSchema.safeParse(await req.json());if(!p.success)return fail('定位数据无效','INVALID_LOCATION');const setting=await db.companySetting.findUnique({where:{id:'company'}});if(setting?.latitude==null||setting.longitude==null)return fail('管理员尚未配置考勤地点','ATTENDANCE_NOT_CONFIGURED');const distance=haversineMeters(p.data,{latitude:setting.latitude,longitude:setting.longitude});if(distance>setting.attendanceRadius)return fail(`当前距离公司 ${Math.round(distance)} 米，超出签退范围`,'OUT_OF_RANGE');const date=startOfChinaDay(),record=await db.attendance.findUnique({where:{userId_date:{userId:user.id,date}}});if(!record?.checkInTime)return fail('请先签到','NOT_CHECKED_IN',409);if(record.checkOutTime)return fail('今天已经签退','ALREADY_CHECKED_OUT',409);const now=new Date(),earlyThreshold=new Date(timeOnChinaDay(date,setting.workEnd).getTime()-3600000),isEarlyLeave=now<earlyThreshold,{status,exceptionType}=attendanceResult(record.isLate,isEarlyLeave),directAdmin=attendanceDirectAdmin(user.role.code,user.department?.code);const result=await db.$transaction(async tx=>{const item=await tx.attendance.update({where:{id:record.id},data:{checkOutTime:now,checkOutLatitude:p.data.latitude,checkOutLongitude:p.data.longitude,checkOutAccuracy:p.data.accuracy,isEarlyLeave,status}}),existingException=await tx.attendanceException.findUnique({where:{attendanceId:item.id}});if(exceptionType&&!existingException)await tx.attendanceException.create({data:{attendanceId:item.id,type:exceptionType,status:directAdmin?'PENDING_ADMIN':'PENDING_MANAGER'}});else if(exceptionType&&existingException&&existingException.type!==exceptionType)await tx.attendanceException.update({where:{id:existingException.id},data:{type:exceptionType,status:directAdmin?'PENDING_ADMIN':'PENDING_MANAGER'}});else if(!exceptionType&&existingException)await tx.attendanceException.delete({where:{id:existingException.id}});await tx.operationLog.create({data:{userId:user.id,action:'CHECK_OUT',module:'ATTENDANCE',targetId:item.id,description:`签退${isEarlyLeave?'（早退）':''}，距离 ${Math.round(distance)} 米`}});return item});return ok(result)}catch(e){return apiError(e)}}
