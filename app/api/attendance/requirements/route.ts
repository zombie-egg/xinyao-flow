import {db} from '@/lib/db';import {requireUser} from '@/lib/auth';import {ok,fail,apiError} from '@/lib/api';import {startOfChinaDay} from '@/lib/utils';import {z} from 'zod';
const schema=z.object({action:z.enum(['CHECK_IN','CHECK_OUT'])});
export async function POST(req:Request){try{const admin=await requireUser();if(admin.role.code!=='ADMIN')return fail('只有管理员可以发布签到签退任务','FORBIDDEN',403);const p=schema.safeParse(await req.json());if(!p.success)return fail('发布类型无效','VALIDATION_ERROR');const date=startOfChinaDay(),now=new Date(),checkIn=p.data.action==='CHECK_IN',requirement=await db.$transaction(async tx=>{const item=await tx.dailyAttendanceRequirement.upsert({where:{date},update:checkIn?{requireCheckIn:true,checkInPublishedAt:now,publishedById:admin.id}:{requireCheckOut:true,checkOutPublishedAt:now,publishedById:admin.id},create:{date,requireCheckIn:checkIn,requireCheckOut:!checkIn,checkInPublishedAt:checkIn?now:null,checkOutPublishedAt:checkIn?null:now,publishedById:admin.id}}),employees=await tx.user.findMany({where:{status:'ACTIVE',departmentId:{not:null}},select:{id:true}});if(employees.length)await tx.notification.createMany({data:employees.map(employee=>({userId:employee.id,type:'SYSTEM' as const,title:checkIn?'今日需要签到':'今日需要签退',content:checkIn?'管理员已发布今日签到任务，请按时签到':'管理员已发布今日签退任务，请按时签退',targetId:item.id}))});await tx.operationLog.create({data:{userId:admin.id,action:checkIn?'PUBLISH_CHECK_IN':'PUBLISH_CHECK_OUT',module:'ATTENDANCE',targetId:item.id,description:checkIn?'发布今日签到':'发布今日签退'}});return item});return ok(requirement)}catch(e){return apiError(e)}}
