import {locationSchema} from '@/lib/validation';import {db} from '@/lib/db';import {requirePermission} from '@/lib/auth';import {ok,fail,apiError} from '@/lib/api';import {haversineMeters,startOfChinaDay} from '@/lib/utils';
export async function POST(req:Request){try{const user=await requirePermission('attendance:self');const parsed=locationSchema.safeParse(await req.json());if(!parsed.success)return fail('定位数据无效','INVALID_LOCATION');const setting=await db.companySetting.findUnique({where:{id:'company'}});if(!setting?.latitude||!setting.longitude)return fail('管理员尚未配置考勤地点','ATTENDANCE_NOT_CONFIGURED');const distance=haversineMeters(parsed.data,{latitude:setting.latitude,longitude:setting.longitude});if(distance>setting.attendanceRadius)return fail(`当前距离公司 ${Math.round(distance)} 米，超出 ${setting.attendanceRadius} 米签到范围`,'OUT_OF_RANGE');const date=startOfChinaDay(),now=new Date();const [h,m]=setting.workStart.split(':').map(Number);const deadline=new Date(date);deadline.setHours(h-8,m,0,0);const status=now<=deadline?'ON_TIME':'LATE';const record=await db.$transaction(async tx=>{const existing=await tx.attendance.findUnique({where:{userId_date:{userId:user.id,date}}});if(existing?.checkInTime)throw new Error('ALREADY_CHECKED_IN');const attendance=await tx.attendance.upsert({where:{userId_date:{userId:user.id,date}},update:{checkInTime:now,checkInLatitude:parsed.data.latitude,checkInLongitude:parsed.data.longitude,checkInAccuracy:parsed.data.accuracy,distanceMeters:distance,status},create:{userId:user.id,date,checkInTime:now,checkInLatitude:parsed.data.latitude,checkInLongitude:parsed.data.longitude,checkInAccuracy:parsed.data.accuracy,distanceMeters:distance,status}});if(status==='LATE')await tx.attendanceException.create({data:{attendanceId:attendance.id,type:'LATE'}});await tx.operationLog.create({data:{userId:user.id,action:'CHECK_IN',module:'ATTENDANCE',targetId:attendance.id,description:`签到 ${status}，距离 ${Math.round(distance)} 米`}});return attendance});return ok({...record,distanceMeters:Math.round(distance)})}catch(e){if(e instanceof Error&&e.message==='ALREADY_CHECKED_IN')return fail('今天已经签到','ALREADY_CHECKED_IN',409);return apiError(e)}}
