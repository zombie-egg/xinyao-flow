import {db} from '@/lib/db';import {requirePermission} from '@/lib/auth';import {ok,fail,apiError} from '@/lib/api';import {fullWorkYears} from '@/lib/work-years';import {z} from 'zod';
const schema=z.object({employmentStartDate:z.coerce.date()});
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{const admin=await requirePermission('user:manage');if(admin.role.code!=='ADMIN')return fail('只有管理员可以修改员工入职时间','FORBIDDEN',403);const {id}=await params,p=schema.safeParse(await req.json());if(!p.success)return fail(p.error.issues[0].message,'VALIDATION_ERROR');const existing=await db.user.findUnique({where:{id}});if(!existing)return fail('员工不存在','NOT_FOUND',404);const workYears=fullWorkYears(p.data.employmentStartDate);if(workYears<0)return fail('入职时间不能晚于今天','INVALID_EMPLOYMENT_DATE',400);if(workYears<Number(existing.annualLeaveUsed))return fail(`入职时间对应年假不能少于已使用年假（${Number(existing.annualLeaveUsed)} 天）`,'ANNUAL_LEAVE_USED_EXCEEDS_TOTAL',409);const updated=await db.user.update({where:{id},data:{employmentStartDate:p.data.employmentStartDate,workYears,annualLeaveDays:workYears}});await db.operationLog.create({data:{userId:admin.id,action:'UPDATE_USER_WORK_INFO',module:'USER',targetId:id,description:`更新员工 ${updated.name} 的入职时间，系统自动计算工龄和年假`}});return ok({id:updated.id,name:updated.name,employmentStartDate:updated.employmentStartDate,workYears:updated.workYears,annualLeaveDays:updated.annualLeaveDays})}catch(e){return apiError(e)}}
