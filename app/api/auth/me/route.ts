import {currentUser} from '@/lib/auth';import {ok,fail} from '@/lib/api';export async function GET(){const u=await currentUser();if(!u)return fail('请先登录','UNAUTHORIZED',401);return ok({id:u.id,name:u.name,username:u.username,role:u.role.code,department:u.department,permissions:u.role.permissions.map(p=>p.permission.code),annualLeaveDays:Number(u.annualLeaveDays),annualLeaveUsed:Number(u.annualLeaveUsed)})}
