import {db} from '@/lib/db';import {requireUser} from '@/lib/auth';import {ok,fail,apiError} from '@/lib/api';export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const u=await requireUser(),{id}=await params,order=await db.order.findUnique({where:{id},include:{customer:true,contract:true,salesUser:{select:{id:true,name:true}},invoice:true,payments:true,approvals:true}});if(!order)return fail('订单不存在','NOT_FOUND',404);const allowed=u.role.code==='ADMIN'||u.role.code.startsWith('FINANCE')||(u.role.code==='TECH_MANAGER'&&order.approvalStatus==='APPROVED'||u.role.code==='TECH_EMPLOYEE'&&order.technicalUserId===u.id)||u.role.code.startsWith('SALES')&&(order.salesUserId===u.id||u.role.code==='SALES_MANAGER'&&order.approvalStatus==='PENDING_SALES_MANAGER');if(!allowed)throw new Error('FORBIDDEN');return ok(order)}catch(e){return apiError(e)}}
