import {db} from '@/lib/db';import {requireUser} from '@/lib/auth';import {ok,fail,apiError} from '@/lib/api';import {z} from 'zod';const schema=z.object({technicalUserId:z.string().min(1)});
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{const manager=await requireUser(),{id}=await params,p=schema.safeParse(await req.json());if(manager.role.code!=='TECH_MANAGER')throw new Error('FORBIDDEN');if(!p.success)return fail('请选择技术员工','VALIDATION_ERROR');const [order,employee]=await Promise.all([db.order.findUnique({where:{id}}),db.user.findFirst({where:{id:p.data.technicalUserId,role:{code:'TECH_EMPLOYEE'},department:{code:'TECH'},status:'ACTIVE'}})]);if(!order||order.approvalStatus!=='APPROVED')return fail('订单尚未通过审核','ORDER_NOT_APPROVED',409);if(order.technicalStatus==='COMPLETED')return fail('技术已完成，不能重新分配','TECH_ALREADY_COMPLETED',409);if(!employee)return fail('技术员工不存在或已禁用','TECH_USER_NOT_FOUND',404);const result=await db.$transaction(async tx=>{const item=await tx.order.update({where:{id},data:{technicalUserId:employee.id,technicalStatus:'PENDING',status:'IN_PROGRESS'}});await tx.notification.create({data:{userId:employee.id,type:'ORDER',title:'新的技术订单任务',content:`${item.orderNumber} · ${item.name}`,targetId:id}});await tx.operationLog.create({data:{userId:manager.id,action:'ASSIGN_TECH_ORDER',module:'ORDER',targetId:id,description:`技术经理将订单分配给 ${employee.name}`}});return item});return ok(result)}catch(e){return apiError(e)}}
