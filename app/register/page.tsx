'use client';import Link from 'next/link';import {useState} from 'react';import {useRouter} from 'next/navigation';import {Button} from '@/components/ui/button';import {Input} from '@/components/ui/input';
export default function Register(){const router=useRouter(),[department,setDepartment]=useState('SALES'),[message,setMessage]=useState(''),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget),res=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f.entries()))}),body=await res.json();setLoading(false);if(!res.ok){setMessage(body.message);return}setMessage('注册成功，正在返回登录页…');setTimeout(()=>router.push('/login'),700)}return <main className="grid min-h-screen place-items-center bg-zinc-950 p-5"><form onSubmit={submit} className="w-full max-w-lg rounded-2xl bg-white p-8 shadow-2xl"><h1 className="text-2xl font-semibold">员工注册</h1><p className="mt-2 text-sm text-zinc-500">财务人员拥有统一财务权限，无需区分员工和经理。管理员账号只能后台创建。</p><div className="mt-7 grid gap-4 sm:grid-cols-2"><label className="text-sm">姓名<Input name="name" className="mt-2" required/></label><label className="text-sm">账号<Input name="username" className="mt-2" required/></label><label className="text-sm">手机号<Input name="phone" className="mt-2" required/></label><label className="text-sm">密码<Input name="password" type="password" className="mt-2" required/></label><label className="text-sm">部门<select name="department" value={department} onChange={e=>setDepartment(e.target.value)} className="mt-2 h-10 w-full rounded-lg border bg-white px-3"><option value="SALES">销售</option><option value="TECH">技术</option><option value="FINANCE">财务</option></select></label>{department!=='FINANCE'&&<label className="text-sm">职位<select name="position" className="mt-2 h-10 w-full rounded-lg border bg-white px-3"><option value="EMPLOYEE">员工</option><option value="MANAGER">经理</option></select></label>}</div>{department==='FINANCE'&&<p className="mt-4 rounded-lg bg-zinc-50 p-3 text-sm text-zinc-600">所有财务人员拥有发票、回款、合同审核、销售业绩和财务统计权限。</p>}{message&&<p className="mt-4 text-sm text-zinc-600">{message}</p>}<Button disabled={loading} className="mt-6 w-full">{loading?'注册中…':'注册'}</Button><Link href="/login" className="mt-4 block text-center text-sm text-zinc-500">已有账号，返回登录</Link></form></main>}
