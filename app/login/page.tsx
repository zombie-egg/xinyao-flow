'use client';import Link from 'next/link';import {useState} from 'react';import {useRouter} from 'next/navigation';import {Button} from '@/components/ui/button';import {Input} from '@/components/ui/input';
export default function Login(){const r=useRouter(),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const fd=new FormData(e.currentTarget),res=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({identifier:fd.get('identifier'),password:fd.get('password')})}),body=await res.json();setLoading(false);if(!res.ok){setError(body.message);return}r.push('/');r.refresh()}return <main className="grid min-h-screen place-items-center bg-zinc-950 p-5"><form onSubmit={submit} className="w-full max-w-sm rounded-2xl bg-white p-8 shadow-2xl"><div className="mb-8"><div className="mb-4 grid size-11 place-items-center rounded-xl bg-zinc-950 font-bold text-white">企</div><h1 className="text-2xl font-semibold tracking-tight">欢迎回来</h1><p className="mt-2 text-sm text-zinc-500">使用邮箱或账号登录</p></div><label className="text-sm font-medium">邮箱或账号</label><Input name="identifier" autoComplete="username" className="mt-2" required/><label className="mt-5 block text-sm font-medium">密码</label><Input name="password" type="password" autoComplete="current-password" className="mt-2" required/>{error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<Button disabled={loading} className="mt-6 w-full">{loading?'正在登录…':'登录'}</Button><Link href="/register" className="mt-5 block text-center text-sm text-zinc-500 hover:text-zinc-900">没有账号？邮箱注册</Link></form></main>}
