import Link from 'next/link';import {requireUser} from '@/lib/auth';import {PageHeader} from '@/components/page';import {Card} from '@/components/ui/card';import {ProfileForm} from '@/components/profile-form';import {db} from '@/lib/db';import {money} from '@/lib/utils';export default async function Me(){const u=await requireUser(),sales=u.role.code.startsWith('SALES'),performance=sales?await db.order.aggregate({where:{salesUserId:u.id,approvalStatus:'APPROVED'},_count:{id:true},_sum:{amount:true}}):null,rows=[['账号',u.username],['部门',u.department?.name||'管理员'],['职位',u.jobTitle],['工龄',`${u.workYears} 年`],['入职时间',u.employmentStartDate.toLocaleDateString('zh-CN')],['年假总天数',`${Number(u.annualLeaveDays)} 天`],['已使用年假',`${Number(u.annualLeaveUsed)} 天`],['剩余年假',`${Number(u.annualLeaveDays)-Number(u.annualLeaveUsed)} 天`]];return <><PageHeader title="我的" description="修改显示名称和头像，信息会同步到左下角"/><ProfileForm name={u.name} avatarUrl={u.avatarUrl}/>{performance&&<Link href="/performance" className="mb-5 grid max-w-2xl grid-cols-2 gap-4"><Card><p className="text-sm text-zinc-500">我的签约数量</p><p className="mt-2 text-2xl font-semibold">{performance._count.id}</p></Card><Card><p className="text-sm text-zinc-500">我的签约金额</p><p className="mt-2 text-2xl font-semibold">{money(Number(performance._sum.amount||0))}</p></Card></Link>}<Card className="max-w-2xl"><dl className="grid gap-x-8 md:grid-cols-2">{rows.map(([k,v])=><div key={k} className="flex justify-between border-t py-4 text-sm"><dt className="text-zinc-500">{k}</dt><dd className="font-medium">{v}</dd></div>)}</dl></Card></>}
