import Link from 'next/link';import {PageHeader,Empty} from '@/components/page';import {AttendancePanel} from '@/components/attendance-panel';import {AttendanceRecords} from '@/components/attendance-records';import {db} from '@/lib/db';import {requirePermission} from '@/lib/auth';import {Card} from '@/components/ui/card';import {startOfChinaDay} from '@/lib/utils';
export default async function Attendance({searchParams}:{searchParams:Promise<{type?:string}>}){const u=await requirePermission('attendance:self'),params=await searchParams,selected=['LATE','EARLY_LEAVE','ABSENT'].includes(params.type||'')?params.type:null,setting=await db.companySetting.findUnique({where:{id:'company'}});if(setting?.latitude==null||setting.longitude==null)return <><PageHeader title="我的考勤" description="公司尚未设置真实考勤位置"/><Card className="mx-auto max-w-xl text-center"><h2 className="text-lg font-semibold">未配置公司真实位置</h2><p className="mt-2 text-sm text-zinc-500">系统已移除默认坐标。在管理员设置真实位置前，不允许签到。</p>{u.role.code==='ADMIN'&&<Link href="/settings" className="mt-5 inline-flex h-10 items-center rounded-lg bg-zinc-950 px-4 text-sm font-medium text-white">前往设置真实位置</Link>}</Card></>;const today=startOfChinaDay(),chinaToday=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Shanghai',year:'numeric',month:'2-digit'}).format(new Date()),month=new Date(`${chinaToday}-01T00:00:00+08:00`),[rows,current,exceptions]=await Promise.all([db.attendance.findMany({where:{userId:u.id,date:{gte:month}}}),db.attendance.findUnique({where:{userId_date:{userId:u.id,date:today}}}),selected?db.attendanceException.findMany({where:{attendance:{userId:u.id,date:{gte:month},status:{not:'LEAVE'}},type:selected as 'LATE'|'EARLY_LEAVE'|'ABSENT'},include:{attendance:true},orderBy:{attendance:{date:'desc'}}}):Promise.resolve([])]),items=[['本月出勤',rows.filter(x=>x.status==='ON_TIME'&&!x.isEarlyLeave).length,null],['迟到次数',rows.filter(x=>x.status==='LATE').length,'LATE'],['早退次数',rows.filter(x=>x.isEarlyLeave&&x.status!=='ABSENT').length,'EARLY_LEAVE'],['旷工次数',rows.filter(x=>x.status==='ABSENT').length,'ABSENT']] as const;return <><PageHeader title="我的考勤" description="上班前 1 小时内可签到；距离下班不足 1 小时签退不计早退"/><AttendancePanel radius={setting.attendanceRadius} workStart={setting.workStart} workEnd={setting.workEnd} company={{latitude:setting.latitude,longitude:setting.longitude}} initialCheckedIn={Boolean(current?.checkInTime)} initialCheckedOut={Boolean(current?.checkOutTime)}/><div className="mx-auto mt-5 grid max-w-5xl grid-cols-2 gap-3 md:grid-cols-4">{items.map(([label,value,type])=>type?<Link key={label} href={`/attendance?type=${type}`}><Card className={`h-full transition hover:border-zinc-400 ${selected===type?'border-zinc-950 ring-1 ring-zinc-950':''}`}><p className="text-xs text-zinc-500">{label}</p><p className="mt-2 text-2xl font-semibold">{value}</p><p className="mt-2 text-xs text-zinc-400">点击查看并填写原因</p></Card></Link>:<Card key={label}><p className="text-xs text-zinc-500">{label}</p><p className="mt-2 text-2xl font-semibold">{value}</p></Card>)}</div>{selected&&<section className="mx-auto mt-7 max-w-5xl"><div className="mb-3 flex items-center justify-between"><h2 className="font-medium">{selected==='LATE'?'迟到':selected==='EARLY_LEAVE'?'早退':'旷工'}记录与原因</h2><Link href="/attendance" className="text-sm text-zinc-500 underline">收起</Link></div>{exceptions.length?<AttendanceRecords items={exceptions.map(x=>({id:x.id,date:x.attendance.date.toISOString(),type:x.type,reason:x.reason,status:x.status,checkInTime:x.attendance.checkInTime?.toISOString()||null,checkOutTime:x.attendance.checkOutTime?.toISOString()||null}))}/>:<Empty text="本月暂无相关记录"/>}</section>}</>}
