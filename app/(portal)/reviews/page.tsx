import {requireUser} from '@/lib/auth';import {db} from '@/lib/db';import {PageHeader,Empty} from '@/components/page';import {Card} from '@/components/ui/card';import {ReviewActions} from '@/components/order-actions';import {money,dateTime} from '@/lib/utils';export default async function Reviews(){const u=await requireUser(),manager=u.role.code==='SALES_MANAGER',finance=u.role.code.startsWith('FINANCE'),admin=u.role.code==='ADMIN';if(!manager&&!finance&&!admin)throw new Error('FORBIDDEN');const where=manager?{approvalStatus:'PENDING_SALES_MANAGER' as const,salesUser:{departmentId:u.departmentId}}:finance?{approvalStatus:'PENDING_FINANCE' as const}:{approvalStatus:'PENDING_ADMIN' as const},items=await db.order.findMany({where,include:{customer:true,salesUser:true,contract:true,approvals:{include:{approver:true},orderBy:{createdAt:'asc'}}},orderBy:{createdAt:'asc'}}),description=manager?'销售经理审核销售员工提交的订单与合同':finance?'财务审核订单金额、客户和合同信息':'管理员在销售经理和财务均通过后进行最终审核';return <><PageHeader title="合同审核" description={description}/>{items.length?<div className="space-y-3">{items.map(x=><Card key={x.id} className="flex flex-wrap items-center justify-between gap-4"><div><a href={`/orders/${x.id}`} className="font-medium hover:underline">{x.name}</a><p className="mt-1 text-sm text-zinc-500">{x.customer.name} · 销售：{x.salesUser.name} · {money(Number(x.amount))}</p><p className="mt-1 text-xs text-zinc-400">提交于 {dateTime(x.createdAt)} · <a className="underline" href={x.contract.fileUrl||'#'} target="_blank">查看合同</a></p>{x.approvals.length>0&&<p className="mt-2 text-xs text-zinc-500">已完成：{x.approvals.map(a=>`${a.stage}（${a.approver.name}）`).join(' → ')}</p>}</div><ReviewActions id={x.id}/></Card>)}</div>:<Empty text="暂无待审核订单"/>}</>}
