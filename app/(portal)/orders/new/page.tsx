import {requirePermission} from '@/lib/auth';import {db} from '@/lib/db';import {PageHeader} from '@/components/page';import {NewOrderForm} from '@/components/new-order-form';export default async function NewOrder(){await requirePermission('order:create');const customers=await db.customer.findMany({select:{id:true,name:true,contact:true,phone:true,address:true,contactInfo:true},orderBy:{name:'asc'}});return <><PageHeader title="新建订单" description="选择客户、填写项目需求、上传合同后提交审核"/><NewOrderForm customers={customers}/></>}
