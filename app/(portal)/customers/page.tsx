import {requirePermission} from '@/lib/auth';import {db} from '@/lib/db';import {PageHeader,Empty} from '@/components/page';import {CustomerManager} from '@/components/customer-manager';export default async function Customers({searchParams}:{searchParams:Promise<{return?:string}>}){const u=await requirePermission('customer:view'),q=await searchParams,items=await db.customer.findMany({include:{owner:{select:{name:true}}},orderBy:{createdAt:'desc'},take:100}),canCreate=u.role.code.startsWith('SALES')||u.role.code==='ADMIN';return <><PageHeader title="客户管理" description="客户资料独立保存，项目需求只属于订单"/><CustomerManager items={items} canCreate={canCreate} returnTo={q.return}/>{!items.length&&<Empty text="暂无客户"/>}</>}
