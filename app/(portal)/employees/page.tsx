import {requirePermission} from '@/lib/auth';import {db} from '@/lib/db';import {PageHeader,Empty} from '@/components/page';import {EmployeeManager} from '@/components/employee-manager';
export default async function Employees(){await requirePermission('user:manage');const users=await db.user.findMany({include:{role:true,department:true},orderBy:{createdAt:'desc'}});return <><PageHeader title="员工管理" description="管理员可以维护员工账号、部门、职位、工龄与入职时间"/>{!users.length?<Empty/>:<EmployeeManager users={users.map(x=>({...x,employmentStartDate:x.employmentStartDate.toISOString(),annualLeaveDays:Number(x.annualLeaveDays),annualLeaveUsed:Number(x.annualLeaveUsed)}))}/>}</>}
