import {requirePermission} from '@/lib/auth';import {db} from '@/lib/db';import {PageHeader,Empty} from '@/components/page';import {EmployeeManager} from '@/components/employee-manager';import {currentWorkInfo} from '@/lib/work-years';
export default async function Employees(){await requirePermission('user:manage');const users=await db.user.findMany({include:{role:true,department:true},orderBy:{createdAt:'desc'}});return <><PageHeader title="员工管理" description="管理员维护员工入职时间，系统按当天日期自动计算工龄和年假"/>{!users.length?<Empty/>:<EmployeeManager users={users.map(x=>({...x,...currentWorkInfo(x.employmentStartDate),employmentStartDate:x.employmentStartDate.toISOString(),annualLeaveUsed:Number(x.annualLeaveUsed)}))}/>}</>}
