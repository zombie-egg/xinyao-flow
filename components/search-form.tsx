import {Search} from 'lucide-react';import {Input} from './ui/input';import {Button} from './ui/button';
export function SearchForm({defaultValue='',placeholder='搜索',hidden={},clearHref='?'}:{defaultValue?:string;placeholder?:string;hidden?:Record<string,string|undefined>;clearHref?:string}){return <form className="mb-5 flex max-w-2xl gap-2">{Object.entries(hidden).map(([name,value])=>value!==undefined&&<input key={name} type="hidden" name={name} value={value}/>)}<div className="relative flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-zinc-400" size={17}/><Input name="q" defaultValue={defaultValue} placeholder={placeholder} className="pl-9"/></div><Button type="submit">搜索</Button>{defaultValue&&<a href={clearHref} className="inline-flex h-10 items-center rounded-lg border bg-white px-4 text-sm">清除</a>}</form>}
