'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import {Button} from './ui/button';import {Input} from './ui/input';import {Card} from './ui/card';type Customer={id:string;name:string;contact:string;phone:string;address:string|null;contactInfo:string|null;owner:{name:string}};
export function CustomerManager({items,canCreate,returnTo}:{items:Customer[];canCreate:boolean;returnTo?:string}){const router=useRouter(),[show,setShow]=useState(Boolean(returnTo)),[message,setMessage]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),res=await fetch('/api/customers',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f.entries()))}),body=await res.json();if(!res.ok){setMessage(body.message);return}if(returnTo){router.push(returnTo);router.refresh();return}setMessage('客户创建成功');setShow(false);router.refresh()}return <>{canCreate&&<div className="mb-5 flex justify-end"><Button onClick={()=>setShow(!show)}>{show?'取消':'新建客户'}</Button></div>}{show&&<Card className="mb-5"><form onSubmit={submit} className="grid gap-4 md:grid-cols-2"><Input name="name" placeholder="客户名称" required/><Input name="contact" placeholder="联系人" required/><Input name="phone" placeholder="联系电话" required/><Input name="contactInfo" placeholder="其他联系方式（微信、邮箱等）"/><Input name="address" placeholder="地址" className="md:col-span-2"/><Input name="remark" placeholder="备注（不填写项目需求）" className="md:col-span-2"/><div className="md:col-span-2"><Button>保存客户</Button>{message&&<span className="ml-3 text-sm text-zinc-500">{message}</span>}</div></form></Card>}<div className="overflow-x-auto rounded-xl border bg-white"><table className="w-full min-w-[850px] text-left text-sm"><thead className="bg-zinc-50 text-zinc-500"><tr>{['客户名称','联系人','联系电话','其他联系方式','地址','创建人'].map(x=><th key={x} className="px-4 py-3 font-medium">{x}</th>)}</tr></thead><tbody>{items.map(c=><tr key={c.id} className="border-t"><td className="px-4 py-4 font-medium">{c.name}</td><td className="px-4">{c.contact}</td><td className="px-4">{c.phone}</td><td className="px-4">{c.contactInfo||'—'}</td><td className="px-4">{c.address||'—'}</td><td className="px-4">{c.owner.name}</td></tr>)}</tbody></table></div></>}
