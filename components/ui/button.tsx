import * as React from 'react'; import { cn } from '@/lib/utils';
export function Button({className,variant='primary',...props}:React.ButtonHTMLAttributes<HTMLButtonElement>&{variant?:'primary'|'outline'|'ghost'|'danger'}){return <button className={cn('inline-flex h-10 items-center justify-center rounded-lg px-4 text-sm font-medium transition disabled:pointer-events-none disabled:opacity-50',variant==='primary'&&'bg-zinc-950 text-white hover:bg-zinc-800',variant==='outline'&&'border bg-white hover:bg-zinc-50',variant==='ghost'&&'hover:bg-zinc-100',variant==='danger'&&'bg-red-600 text-white',className)} {...props}/>}
