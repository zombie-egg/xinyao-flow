'use client';import Link from 'next/link';import {useState} from 'react';import {useRouter} from 'next/navigation';import {Button} from './ui/button';import {Input} from './ui/input';import {Badge} from './ui/badge';import {roleText,userStatusText} from '@/lib/chinese-labels';
type Employee={id:string;name:string;username:string;phone:string|null;status:string;jobTitle:string;employmentStartDate:string;workYears:number;annualLeaveDays:number|string;annualLeaveUsed:number|string;department:{name:string}|null;role:{code:string}};
function dateValue(value:string){return new Date(value).toISOString().slice(0,10)}
export function EmployeeManager({users,canEdit}:{users:Employee[];canEdit:boolean}){const router=useRouter(),[editing,setEditing]=useState<string|null>(null),[message,setMessage]=useState(''),[saving,setSaving]=useState(false);async function save(id:string,e:React.FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);setMessage('');const data=Object.fromEntries(new FormData(e.currentTarget).entries()),res=await fetch(`/api/users/${id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify(data)}),body=await res.json();setSaving(false);if(!res.ok){setMessage(body.message);return}setMessage(`员工信息已保存，工龄自动计算为 ${body.data.workYears} 年`);setEditing(null);router.refresh()}return <div className="overflow-x-auto rounded-xl border bg-white"><table className="w-full min-w-[1150px] text-left text-sm"><thead className="bg-zinc-50 text-zinc-500"><tr>{['姓名','账号','部门','角色','职位','工龄','入职时间','年假','状态','考勤','操作'].map(x=><th key={x} className="px-4 py-3 font-medium">{x}</th>)}</tr></thead><tbody>{users.map(x=><tr key={x.id} className="border-t align-top"><td className="px-4 py-4 font-medium">{x.name}</td><td className="px-4 py-4">{x.username}</td><td className="px-4 py-4">{x.department?.name||'管理员'}</td><td className="px-4 py-4">{roleText[x.role.code]}</td><td className="px-4 py-4">{x.jobTitle}</td><td className="px-4 py-4">{x.workYears} 年</td>{canEdit&&editing===x.id?<td className="px-4 py-3"><form onSubmit={e=>save(x.id,e)} className="flex flex-wrap items-center gap-2"><Input name="employmentStartDate" type="date" defaultValue={dateValue(x.employmentStartDate)} className="w-40" aria-label="入职时间" required/><Button disabled={saving}>{saving?'保存中…':'保存'}</Button><Button type="button" variant="ghost" onClick={()=>setEditing(null)}>取消</Button></form></td>:<td className="px-4 py-4">{dateValue(x.employmentStartDate)}</td>}<td className="px-4 py-4">{Number(x.annualLeaveUsed)} / {Number(x.annualLeaveDays)} 天</td><td className="px-4 py-4"><Badge>{userStatusText[x.status]}</Badge></td><td className="px-4 py-4"><Link href={`/employees/${x.id}/attendance`} className="text-sm font-medium underline underline-offset-4">查看考勤</Link></td><td className="px-4 py-4">{canEdit?(editing===x.id?message||'':<Button variant="outline" onClick={()=>{setMessage('');setEditing(x.id)}}>编辑入职时间</Button>):<span className="text-zinc-400">只读</span>}</td></tr>)}</tbody></table>{message&&editing===null&&<p className="border-t px-4 py-3 text-sm text-zinc-500">{message}</p>}</div>}
